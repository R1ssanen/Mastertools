#include "clipping.hpp"

namespace mt {

    bool intersect_line_aa_plane(
        const glm::vec4& p0, const glm::vec4& p1, AAClipPlane plane, glm::vec4& intersection
    ) {

        switch (plane) { case AAClipPlane::LEFT: }
    }

} // namespace mt
